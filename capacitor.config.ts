import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'FotosgramTutu',
  /* webDir: 'www', */
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
